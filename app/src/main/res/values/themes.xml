<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.MoneyMind" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- 🎨 Основные цвета -->
        <item name="colorPrimary">@color/md_theme_light_primary</item>
        <item name="colorOnPrimary">@color/md_theme_light_onPrimary</item>
        <item name="colorSecondary">@color/md_theme_light_secondary</item>
        <item name="colorOnSecondary">@color/md_theme_light_onSecondary</item>

        <!-- 📦 Поверхности -->
        <item name="colorSurface">@color/md_theme_light_surface</item>
        <item name="colorOnSurface">@color/md_theme_light_onSurface</item>
        <item name="android:colorBackground">@color/md_theme_light_background</item>
        <item name="colorError">@color/md_theme_light_error</item>

        <!-- 🧭 Навигация и статус -->
        <item name="android:statusBarColor">@color/md_theme_light_background</item>
        <item name="android:navigationBarColor">@color/md_theme_light_background</item>

        <!-- 📊 Цвета для вкладок (TabLayout) -->
        <item name="chartTabSelectedTextColor">#000000</item>  <!-- Цвет для текста выбранной вкладки -->
        <item name="chartTabIndicatorColor">#C792EA</item>  <!-- Цвет индикатора вкладки -->

        <!-- 🚀 Исправляем белую вспышку -->
        <item name="android:windowBackground">@color/md_theme_light_background</item>
        <item name="android:windowDisablePreview">true</item>
    </style>

</resources>