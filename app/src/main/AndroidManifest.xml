<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".MoneyMindApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoneyMind"
        tools:targetApi="31">

        <!-- Главное окно -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Остальные активити -->
        <activity android:name=".ui.StatsActivity" />
        <activity android:name=".ui.AddExpenseActivity" />
        <activity android:name=".ui.AddCustomCategoryActivity" />
        <activity android:name=".ui.ChooseExpenseCategoryActivity" />
        <activity android:name=".ui.ChooseIncomeCategoryActivity" />

        <!-- Если есть активити, которые ещё нужны -->
        <!-- <activity android:name=".ui.choose.ChooseCategoryActivity" /> -->
    </application>

</manifest>